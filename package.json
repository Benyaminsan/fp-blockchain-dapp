{
  "name": "project-2-smart-contract",
  "version": "1.0.0",
  "description": "Simple Voting smart contract for Project 2",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "chai": "^4.3.7",
    "ethers": "^6.8.0",
    "mocha": "^10.2.0"
  }
}
